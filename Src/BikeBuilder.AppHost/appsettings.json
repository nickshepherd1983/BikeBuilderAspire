{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Aspire.Hosting.Dcp": "Warning"
    }
  },
  "ConnectionStrings": {
    // The assistant's model host: the Ollama installed on this machine and the model to use.
    // Override per machine with user secrets (dotnet user-secrets set ConnectionStrings:ollama
    // "Endpoint=http://localhost:11434;Model=qwen3.5:35b-a3b" --project Src/BikeBuilder.AppHost).
    "ollama": "Endpoint=http://localhost:11434;Model=qwen3.5"
  }
}
