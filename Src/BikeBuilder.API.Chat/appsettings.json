{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Ollama": {
    // Endpoint and model normally arrive as the "ollama" connection string the AppHost injects
    // (Endpoint=...;Model=...); these tune the completions.
    "Think": false,
    "Temperature": 0.2
  }
}
