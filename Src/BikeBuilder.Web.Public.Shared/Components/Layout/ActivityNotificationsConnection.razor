@* Renders nothing - owns the SignalR connection that surfaces the public activity feed
   (new components, new bike builds, ratings, orders) as toasts. Mounted in MainLayout so
   the toasts follow the visitor across every page, not just the storefront. *@
