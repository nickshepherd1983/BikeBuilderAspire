@* Renders nothing - owns the SignalR connection that surfaces order toasts while the user
   is signed in. Mounted inside MainLayout's AuthorizeView, so its lifecycle (connect on
   login, dispose on logout) falls out of the auth state. *@
