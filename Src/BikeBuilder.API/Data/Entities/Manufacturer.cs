namespace BikeBuilder.API.Data.Entities;

// Values numbered to match the proto Manufacturer enum so the two cast directly.
public enum Manufacturer
{
  Other = 0,
  Sram = 1,
  Shimano = 2,
  Hope = 3
}
